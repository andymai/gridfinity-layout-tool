/**
 * Types Index - Re-exports for convenient imports
 *
 * Import generation types from here:
 *   import type { BinModelConfig, BaseplateConfig } from './types';
 *
 * Or import from the specific module:
 *   import type { BinModelConfig } from './types/generation';
 */

export * from './generation';
