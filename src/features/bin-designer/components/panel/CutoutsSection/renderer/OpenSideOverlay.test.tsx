import { describe, it, expect } from 'vitest';
import { OpenSideOverlay } from './OpenSideOverlay';

describe('OpenSideOverlay', () => {
  it('exports a function', () => {
    expect(typeof OpenSideOverlay).toBe('function');
  });
});
