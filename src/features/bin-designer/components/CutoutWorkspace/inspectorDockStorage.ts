/**
 * Width bounds for the cutout inspector dock. Persistence itself moved into
 * the design system's SidePanel (persistKey "gridfinity-cutout-inspector"
 * reproduces the original localStorage keys, so saved widths carried over).
 */

export const INSPECTOR_MIN_WIDTH = 220;
export const INSPECTOR_MAX_WIDTH = 420;
