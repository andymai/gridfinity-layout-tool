import { describe, expect, it } from 'vitest';
import { WorkshopCanvas } from './WorkshopCanvas';

describe('WorkshopCanvas', () => {
  it('exports a function', () => {
    expect(typeof WorkshopCanvas).toBe('function');
  });
});
