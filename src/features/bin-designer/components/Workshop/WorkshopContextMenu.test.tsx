import { describe, expect, it } from 'vitest';
import { WorkshopContextMenu } from './WorkshopContextMenu';

describe('WorkshopContextMenu', () => {
  it('exports a function', () => {
    expect(typeof WorkshopContextMenu).toBe('function');
  });
});
