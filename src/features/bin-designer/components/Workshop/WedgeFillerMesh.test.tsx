import { describe, expect, it } from 'vitest';
import { WedgeFillerMesh } from './WedgeFillerMesh';

describe('WedgeFillerMesh', () => {
  it('exports a function', () => {
    expect(typeof WedgeFillerMesh).toBe('function');
  });
});
