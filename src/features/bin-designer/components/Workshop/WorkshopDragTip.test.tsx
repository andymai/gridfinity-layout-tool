import { describe, expect, it } from 'vitest';
import { WorkshopDragTip } from './WorkshopDragTip';

describe('WorkshopDragTip', () => {
  it('exports a function', () => {
    expect(typeof WorkshopDragTip).toBe('function');
  });
});
