import { describe, expect, it } from 'vitest';
import { PartProxyMesh } from './PartProxyMesh';

describe('PartProxyMesh', () => {
  it('exports a function', () => {
    expect(typeof PartProxyMesh).toBe('function');
  });
});
