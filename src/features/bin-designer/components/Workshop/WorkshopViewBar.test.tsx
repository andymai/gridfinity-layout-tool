import { describe, expect, it } from 'vitest';
import { WorkshopViewBar } from './WorkshopViewBar';

describe('WorkshopViewBar', () => {
  it('exports a function', () => {
    expect(typeof WorkshopViewBar).toBe('function');
  });
});
