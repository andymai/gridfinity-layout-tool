import { describe, expect, it } from 'vitest';
import { WorkshopScene } from './WorkshopScene';

describe('WorkshopScene', () => {
  it('exports a function', () => {
    expect(typeof WorkshopScene).toBe('function');
  });
});
